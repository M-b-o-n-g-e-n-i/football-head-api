# football-head-api
A backend service built in Go that ingests match data and exposes REST endpoints to compute league and team statistics. The project focuses on backend fundamentals including HTTP servers, data aggregation, error handling, and clean API design, with an emphasis on performance and simplicity.
